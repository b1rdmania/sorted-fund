{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "login:build": "cd frontend/dashboard-v2/login-app && npm install && npm run build",
    "build": "npm run docs:build && npm run login:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
